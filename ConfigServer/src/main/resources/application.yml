server:
  port: 8081

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SantiagoCz/GymCore.git
          search-paths: config-data
          default-label: main
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}